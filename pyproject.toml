[project]
name = "prono-pl"
version = "0.1.0"
description = "Premier League match prediction pipeline — scrapes Understat data, builds ranking/EWP/team-power features, fits a Dixon-Coles model and predicts upcoming match scores."
authors = [
    { name = "Vincent Mouillot" }
]
license = { text = "MIT" }
requires-python = ">=3.12"

# Runtime dependencies are pinned in requirements.txt (single source of truth).
